
const config = {
    debugMode: true, //verbose store output not for prod!!!
    rootUri: "https://chatapi.fake.org/api/", 
    storeSessionStorageKey: "nrcwv1_cfg" // const linked to the widget script tag as deployed / see public/index.html
}

export default config;